---
import SignInButton from "./SignInButton";
import { HeaderNavbar } from "./HeaderNavbar";

const { user, permissions } = Astro.locals;
---

<HeaderNavbar client:load userData={user} permissions={permissions!} />
